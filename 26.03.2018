<!doctype html>
<html>
<head>
<meta charset="utf-8">
<style>
*
{
	padding:0;
	margin:0px;
}
body {
	background: black;
	color: white;
	font-size: 35px;
}
button{
	font-size: 50px;
}
.clock {
	position: absolute;
	top: 50%;
	left: 50%;
	transform: translateX(-50%) translateY(-50%);
	color: lightgreen;
	font-size: 60px;
	font-family: Arial;
	letter-spacing: 7px;
}
</style>

</head>

<body>
	<ul id="list"></ul>
	<button onclick="addLi()">Add item</button>
	<div class="clock"></div>
</body>

<script src="js.js"></script>
</html>


// // console.log("*********************")
// // 1. Chunky Monkey (FCC)

// // console.log("*********************")
// // // 2. Slasher Flick (FCC)
function slasher(arr, howMany) {
	var newArr = arr.slice(howMany);
	return newArr;
}
console.log(slasher([1, 2, 3, 4, 5, 6, 7, 8, 9], 1));
console.log(slasher(["text1", "text2", "text3", "text4"], 2));

console.log("*********************")
// // 3. Дан ul, в нем несколько li. Под ul сделайте кнопку, по нажатию на которую в конец ul будет добавляться новый li с текстом 'пункт'. Сделайте так, чтобы при клике на каждый li, ему в конец добавлялся '!'. Это должно работать и для вновь добавленных li. Задачу решите с помощью делегирования (то есть событие должно быть навешано на ul)


console.log("*********************")
// // // 4. Дан массив с числами. Проверьте, есть ли в нем два одинаковых числа подряд. Если есть - выведите 'да', а если нет - выведите 'нет'
var arr = [1, 2, 3, 4, 4, 5, 6, 7, 8, 9, 10], res;
res = "no";
for(i = 0; i < arr.length; i+=1) {
	if(i > 0 && arr[i] === arr[i - 1]){
		res = "yes";
		break;
	}
}
console.log(res);

console.log("*********************")
// // 5. Сделайте функцию getDigitsSum (digit - это цифра), которая параметром принимает целое число и возвращает сумму его цифр.
function getDigitsSum(digit) {
	var i, sum = digit;
	for(i = digit; i > 0; i -=1) {
		digit--;
		sum += digit;
	}return sum;
}
console.log(getDigitsSum(2));
console.log(getDigitsSum(4));
console.log(getDigitsSum(6));

console.log("*********************")
// 6. Проект "Электронные часы" показываете на занятии.
function clock() {
	var now = new Date(),    
	hours = now.getHours(),
	minutes = now.getMinutes(),
	seconds = now.getSeconds();

	document.querySelectorAll(".clock")[0].innerHTML = checkTime(hours) + ":" + checkTime(minutes) + ":" + checkTime(seconds);

	function checkTime(i) {
		if (i < 10) {
			i = "0" + i
		} return i ;
	}
}
setInterval(clock, 500);
